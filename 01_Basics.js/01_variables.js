const AccountId = 99;
var name = "Tanush";
let email= "tanush@gmail.com";
hello = "world";

//AccountId = 77; this is not allowed since it is a constant which cant be further moderated.

console.log([AccountId,name,email,hello]);
// prefer not to use var because it has scope problems.

console.table([AccountId,name,email,hello]);